from srv6 import encap, monitor, routing

encap_config = 3
encap_param = 1

def myChain():
    ip_type = true
    i = 0
    while ip_type == true:
        if i == 8:
            ip_type = false
        i = i + 1
        if encap_config == i:
            encap_param = i + 5 
